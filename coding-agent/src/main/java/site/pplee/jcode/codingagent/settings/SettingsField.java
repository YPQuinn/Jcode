package site.pplee.jcode.codingagent.settings;

/** Stable first-version settings fields used for source reporting and updates. */
public enum SettingsField {
    DEFAULT_MODEL,
    DEFAULT_THINKING_LEVEL,
    DEFAULT_TOOLS,
    STEERING_MODE,
    FOLLOW_UP_MODE,
    REQUEST_MAX_OUTPUT_TOKENS,
    REQUEST_TEMPERATURE
}

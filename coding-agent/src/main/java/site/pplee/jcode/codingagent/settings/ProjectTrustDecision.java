package site.pplee.jcode.codingagent.settings;

/** Headless decision governing whether this project's settings may be loaded. */
public enum ProjectTrustDecision {
    ALLOW,
    DENY,
    UNSPECIFIED
}

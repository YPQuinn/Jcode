package site.pplee.jcode.protocol;

/** Confirmed application outcome of a supplemental input. */
public enum InputStatus {
    PENDING,
    APPLIED_TO_CONTEXT,
    NOT_APPLIED,
    RECONCILIATION_REQUIRED
}

# 07. 扩展路线

> 状态：待补充。

本章用于解释未来模块的扩展入口与创建门槛，包括 provider adapter、coding-agent、server、tui 等方向。

## 待回答的问题

- 新 provider 模块应该依赖哪些内核抽象？
- 什么时候才应该创建 `coding-agent` 等产品层模块？
- 哪些能力不应提前内建到 `agent-core`？
- 如何避免未来扩展破坏当前模块边界？
